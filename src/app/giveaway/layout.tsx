import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Win a Pair of Sporting Sunglasses — Giveaway",
  description: "Enter InnerMe's giveaway for a chance to win a pair of Sporting Sunglasses. Complete a free financial planning session to qualify.",
};

export default function GiveawayLayout({ children }: { children: React.ReactNode }) {
  return children;
}
